# jhamtani-cms

CMS with a React frontend (`CMS-FE`) and Express backend (`CMS-BE`).

## Run locally

Backend:

```bash
cd CMS-BE
npm install
npm run dev
```

Frontend:

```bash
cd CMS-FE
npm install
npm run dev
```

Copy `CMS-BE/.env.example` to `CMS-BE/.env` and set `PORT` and `MONGO_URI`.
